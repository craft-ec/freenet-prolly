[package]
name = "wasm-check"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
freenet-prolly = { path = ".." }
blake3 = { version = "1", default-features = false }

[workspace]
